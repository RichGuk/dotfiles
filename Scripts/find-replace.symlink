# :set ft=sh
#!/bin/sh
#
# Find and replace by a given list of files.
#
# replace foo bar **/*.rb

meh_this=$1
shift
replace_with=$1
shift

ag -l $meh_this $* | xargs sed -i '' "s/$meh_this/$replace_with/g"
