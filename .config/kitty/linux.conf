font_size 13.0

# Hyprland grabs super+w and re-sends ctrl+shift+w to terminals, so that the
# same key can reach Firefox as ctrl+w. close_tab, not kitty's default
# close_window -- the two halves have to agree.
map ctrl+shift+w close_tab
