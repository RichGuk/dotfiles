# vim: ft=gitconfig:
# Only what differs from ~/.config/git/config. Signing key, name and
# commit.gpgsign are inherited; add a signingkey here to sign work with a
# separate key.

[user]
  email = rich@zeneducate.com
